{
  "name": "krishi-sahay",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm run build",
    "dev": "cd client && npm run dev"
  },
  "engines": {
    "node": "18.x"
  }
}
